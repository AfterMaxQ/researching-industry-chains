# 项目指令

## 业务背景

本项目提供一个独立、跨 Agent 的产业链检索与交付数据客户端。研究 Agent 负责搜索、浏览器操作、视觉读图、证据判断和九字段记录生成；Client 负责主题快照、领取租约、确定性校验、稳定 ID、原子修改、JSON 持久化和 XLSX 投影。

项目不依赖其他产业链仓库，不绑定任何 Agent SDK。调用方在创建 Runner 时传入外部 `topic_identity.yaml`，Runner 保存当时的主题目录快照，后续外部配置变化不影响已有批次。

## 九字段数据

每条业务记录固定包含以下字符串字段：

```text
主题、信源主体、分类1、分类2、分类3、分类4、公司、信源URL、备注
```

一行表示一条由来源直接支持的独立节点路径及其企业集合，不表示一家企业。相同路径的多家企业放在同一公司字段，用顿号连接。没有企业的有效节点仍保留，公司字段为空。

分类1至分类4按来源原始层级填写。分类不能中间断层；超过四级时，第四级及其后层级按顺序合并到分类4。原始节点名称不标准化、不润色、不补全。组合标题保持整体，独立叶子节点拆成独立行。

## 来源边界与资格

一个网页、报告或 PDF 独立形成一个来源组。同一文档内的产业链图、后续企业图、表格、企业列表和正文可以综合；其他 URL 或其他报告的节点和企业不得补入。同一底层文档的转载或托管版本只保留一组。

合格来源必须同时具备：

1. 可识别的产业链结构；
2. 至少一组能够明确归属到已有节点的企业证据。

产业链允许不完整，图中无企业的其他节点仍完整保留。只有企业名单而没有节点归属关系的来源不合格。

信源主体按发布关系填写：原始发布写原始主体；转载或托管写`发布平台（原始主体）`；原始主体无法识别时写`发布平台（原始主体未明）`。优先使用2024年及以后来源；较早来源和未识别发布日期在来源组首行备注说明。

## 视觉与证据规则

用于节点或企业判断的产业链图、企业图、表格图和 PDF 页面必须由具备视觉能力的 Agent 实际查看。OCR、正文抓取、搜索摘要或图片下载成功不能替代视觉检查。找到第一张产业链图后必须继续扫描完整网页或报告，直到后续内容不再提供新节点或可归属企业。

产业链结构优先级为：图片或表格，其次是正文的明确结构化表述。普通产品介绍和业务描述不能生成节点。

企业归属优先级为：产业链图明确位置、企业列表、正文明确介绍。企业列表必须通过标题、分组或字段对应已有节点；正文必须明确表达企业提供、生产或布局该节点产品或服务。证据只到父节点时只挂父节点，不能向子节点继承。无法消解的冲突不强行选择。

不得用外部知识、企业知名度、主营业务印象或股票代码推断企业归属。来源内部无法确认全称时，保留图中品牌名或简称。

## Client 边界

模型写入载荷只允许：

```json
{"records": [{"主题": "", "信源主体": "", "分类1": "", "分类2": "", "分类3": "", "分类4": "", "公司": "", "信源URL": "", "备注": ""}]}
```

Client 只执行确定性结构校验，不判断主题相关性、节点正确性或企业归属真实性。来源组必须共享主题、信源主体和 URL；只有第一行允许备注；至少一行公司非空；URL 必须可生成超链接。

每个来源完整解析后一次写入，不能边读边逐行提交。Client 为来源组和行生成内部稳定 ID，支持 `get、insert、patch、replace、remove`，作用域为`topic、source_group、row`。内部 ID、时间和顺序不进入九列 XLSX。

状态修改只提交 `runner.json`。业务数据修改同时原子提交 JSON 和 XLSX。XLSX 是交付投影，不是编辑数据库；所有修改以 Runner JSON 为准。

## Runner、状态和租约

主题状态为`pending、in_progress、completed、no_qualified_source、failed`。`remaining`统计`pending + failed`。自动处理只领取`pending`主题；失败主题由明确补跑重新领取。

`topic claim-next`和`topic claim`返回领取令牌及到期时间。`in_progress`主题的数据修改、结束和失败都必须携带当前有效令牌，合法数据写入会续租。有效租约期间其他研究 Agent 不能重复领取；过期后可重新领取并生成新令牌。

`completed`要求至少一个来源组，`no_qualified_source`要求没有来源组。终态主题可以显式重开。审核 Agent 可以直接修改终态数据，但不能破坏终态与来源组数量的一致性。

每个 Runner 位于`runs/<runner_id>/`，只保存`runner.json`和`<runner_id>_交付数据.xlsx`。不同 Runner 的快照、状态和结果相互隔离。

## 项目范围

保持单一 Python 包和 CLI + JSON 协议。不得引入 SQLite 或其他数据库、知识图谱、来源评分平台、Agent SDK 依赖、模型推理日志、置信度字段、搜索过程或截图持久化。不要把主题相关性、节点正确性或企业真实性判断移入 Client。

Python 包名、模块名、函数变量名、第三方库名和 CLI 命令使用兼容的英文标识。文档、Skill、注释、docstring、错误信息、测试名称和用户可见内容使用中文。文档只描述当前项目和稳定业务规则，不记录修改历史、会话过程、旧版本比较或路线图。

## 实施与验收

改动保持最小且可验证，不重构无关代码。使用 `apply_patch`编辑项目文件，精确暂存批准路径，不使用全量暂存。保留用户已有和无关修改。

验收关注实际行为：外部主题配置可创建快照；并发领取不会产生两个有效持有者；来源组原子写入；三种作用域的五种操作可定位修改；状态和数据约束一致；XLSX 只有九个业务列，保留来源顺序并把 URL 生成为超链接。
